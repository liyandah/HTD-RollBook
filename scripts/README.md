# Scripts

Place non-secret automation scripts here (e.g. DB helpers, build helpers).

Do not commit scripts that hardcode SSH passwords, API tokens, or database credentials.
Use environment variables instead, for example:

```bash
export SSH_HOST=...
export SSH_USER=...
export SSH_PASSWORD=...
```
