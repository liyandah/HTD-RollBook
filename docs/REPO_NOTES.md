# Note on repository history

The first commit message incorrectly said "Angular". This project is **React + Vite** (`frontend/`) with a **Spring Boot** backend. See root `README.md` and `GITHUB_DEPLOYMENT.md`.