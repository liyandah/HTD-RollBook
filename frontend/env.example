# API origin only (no trailing slash, no /api suffix).
# Leave empty for Vite proxy in local development.
#
# Production (Vercel): set the same value in Vercel → Environment Variables
# and redeploy. See docs/CLOUDFLARE-SETUP.md.
VITE_API_BASE_URL=https://api.htdrollbook.com